# ダークモード対応 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Colorackにライト/ダーク/システム追従の3モードのダークモード対応を追加する。

**Architecture:** `lib/theme.ts` に `lightColors`/`darkColors` の2パレットと、`lib/i18n.ts` の `useLocale()` と同型の listener-based `useTheme()` フックを実装する。`app_settings` テーブルに `theme_mode` を永続化し、`app/_layout.tsx` の初期化フェーズで読み込む。既存の19ファイル・125箇所の `colors.x` 直参照(モジュール直下の静的 `StyleSheet.create`)を、コンポーネント内で `useTheme()` から取得した `colors` を使う形に移植する。

**Tech Stack:** Expo SDK ~54 / React Native 0.81 / TypeScript / expo-sqlite。新規npm依存は追加しない(`react-native`標準の`Appearance`APIのみ使用)。

## Global Constraints

- 新規依存パッケージは追加しない([spec](../specs/2026-07-02-dark-mode-design.md) 記載の通り、React標準機能で完結させる)。
- 既存の `colors.x` の値・キー名は変更しない(ライト側は現行値をそのまま `lightColors` に移す)。
- 自動テスト基盤(jest等)は新規導入しない。`lib/theme.ts` の判定ロジックのみ `__DEV__` 時の assert 自己チェックを1つ追加する。
- ブランチ `feature/dark-mode` 上で作業し、mainには直接コミットしない(既存のプロジェクトルール)。

---

## Task 1: lib/theme.ts にダークモード基盤を実装する

**Files:**
- Modify: `lib/theme.ts`(全体)

**Interfaces:**
- Produces:
  - `export type ThemeMode = 'light' | 'dark' | 'system';`
  - `export function resolveIsDark(mode: ThemeMode, systemScheme: 'light' | 'dark' | null | undefined): boolean;`(純粋関数)
  - `export function useTheme(): { colors: typeof lightColors; mode: ThemeMode; isDark: boolean };`
  - `export function setThemeMode(mode: ThemeMode): void;`
  - `export function initTheme(): Promise<void>;`
  - `export const spacing`, `export const radius`, `export const touch` は変更なし(既存のまま)。

- [ ] **Step 1: 現在の `lib/theme.ts` を読み、`colors` の中身を確認する**

現状(2026-07-02時点)の `colors` は以下の23キーを持つ:
`primary, primarySoft, primaryDisabled, danger, dangerSoft, inUse, usedUp, darkAction, neutralAction, surface, surfaceAlt, chip, chipAlt, border, borderLight, text, textSecondary, textMuted, textFaint, textPlaceholder, onPrimary, transparent`

- [ ] **Step 2: `lib/theme.ts` を書き換える**

```typescript
import { useEffect, useReducer } from 'react';
import { Appearance, ColorSchemeName } from 'react-native';
import { getSetting, setSetting } from './db';

export const lightColors = {
  primary: '#4a90d9',
  primarySoft: '#eef4fb',
  primaryDisabled: '#b7cde6',
  danger: '#e74c3c',
  dangerSoft: '#fdecea',
  inUse: '#e67e22',
  usedUp: '#95a5a6',
  darkAction: '#34495e',
  neutralAction: '#7f8c8d',
  surface: '#fff',
  surfaceAlt: '#f5f5f5',
  chip: '#f0f0f0',
  chipAlt: '#e8e8e8',
  border: '#ccc',
  borderLight: '#eee',
  text: '#333',
  textSecondary: '#555',
  textMuted: '#666',
  textFaint: '#888',
  textPlaceholder: '#999',
  onPrimary: '#fff',
  transparent: 'transparent',
};

export const darkColors: typeof lightColors = {
  primary: '#5b9bdb',
  primarySoft: '#1c2b3a',
  primaryDisabled: '#3a4a5a',
  danger: '#e74c3c',
  dangerSoft: '#3a2422',
  inUse: '#e67e22',
  usedUp: '#7f8c8d',
  darkAction: '#4a6178',
  neutralAction: '#95a5a6',
  surface: '#1c1c1e',
  surfaceAlt: '#2c2c2e',
  chip: '#2c2c2e',
  chipAlt: '#3a3a3c',
  border: '#48484a',
  borderLight: '#3a3a3c',
  text: '#f2f2f7',
  textSecondary: '#d1d1d6',
  textMuted: '#aeaeb2',
  textFaint: '#8e8e93',
  textPlaceholder: '#8e8e93',
  onPrimary: '#fff',
  transparent: 'transparent',
};

export type ThemeMode = 'light' | 'dark' | 'system';

export function resolveIsDark(mode: ThemeMode, systemScheme: ColorSchemeName): boolean {
  if (mode === 'light') return false;
  if (mode === 'dark') return true;
  return systemScheme === 'dark';
}

if (__DEV__) {
  console.assert(resolveIsDark('light', 'dark') === false, 'resolveIsDark: light mode must stay light');
  console.assert(resolveIsDark('dark', 'light') === true, 'resolveIsDark: dark mode must stay dark');
  console.assert(resolveIsDark('system', 'dark') === true, 'resolveIsDark: system mode should follow system (dark)');
  console.assert(resolveIsDark('system', 'light') === false, 'resolveIsDark: system mode should follow system (light)');
  console.assert(resolveIsDark('system', null) === false, 'resolveIsDark: system mode with unknown scheme falls back to light');
}

const THEME_MODE_KEY = 'theme_mode';
let currentMode: ThemeMode = 'system';
let systemScheme: ColorSchemeName = Appearance.getColorScheme();
const listeners = new Set<() => void>();

function notify(): void {
  listeners.forEach((l) => l());
}

Appearance.addChangeListener(({ colorScheme }) => {
  systemScheme = colorScheme;
  if (currentMode === 'system') notify();
});

export async function initTheme(): Promise<void> {
  try {
    const saved = await getSetting(THEME_MODE_KEY);
    if (saved === 'light' || saved === 'dark' || saved === 'system') {
      currentMode = saved;
    }
  } catch (e) {
    console.error('initTheme: failed to load theme_mode, falling back to system', e);
  }
}

export function setThemeMode(mode: ThemeMode): void {
  currentMode = mode;
  notify();
  setSetting(THEME_MODE_KEY, mode).catch((e) => console.error('setThemeMode: failed to persist', e));
}

export function getThemeMode(): ThemeMode {
  return currentMode;
}

export function useTheme(): { colors: typeof lightColors; mode: ThemeMode; isDark: boolean } {
  const [, force] = useReducer((x) => x + 1, 0);
  useEffect(() => {
    listeners.add(force);
    return () => { listeners.delete(force); };
  }, []);
  const isDark = resolveIsDark(currentMode, systemScheme);
  return { colors: isDark ? darkColors : lightColors, mode: currentMode, isDark };
}

export const spacing = {
  xs: 4,
  sm: 6,
  md: 8,
  lg: 12,
  xl: 16,
  xxl: 24,
};

export const radius = {
  sm: 6,
  md: 8,
  pill: 16,
  fab: 28,
};

export const touch = {
  min: 44,
};
```

- [ ] **Step 3: TypeScriptの型チェックを実行する**

Run: `cd F:\ClaudeWorks\DevelopColorack && npx tsc --noEmit`
Expected: `lib/theme.ts` に起因するエラーが出ないこと(他ファイルは後続タスクで直すまでエラーが出続けるのが正常)。まだ `colors` を直接importしている19ファイルで `colors` が未定義エラーになるので、この時点では大量のエラーが出て問題ない。

- [ ] **Step 4: コミット**

```bash
git add lib/theme.ts
git commit -m "feat: add dark/light theme palettes and useTheme hook"
```

---

## Task 2: app/_layout.tsx でテーマ初期化とStatusBar追従を行う

**Files:**
- Modify: `app/_layout.tsx`

**Interfaces:**
- Consumes: `initTheme(): Promise<void>` と `useTheme(): { isDark: boolean }`(Task 1で定義)

- [ ] **Step 1: `app/_layout.tsx` を書き換える**

```typescript
// app/_layout.tsx
import { useEffect, useState } from 'react';
import { View, ActivityIndicator } from 'react-native';
import { GestureHandlerRootView } from 'react-native-gesture-handler';
import { Stack } from 'expo-router';
import { StatusBar } from 'expo-status-bar';
import { initDB } from '../lib/db';
import { initTheme, useTheme } from '../lib/theme';

export default function RootLayout() {
  // initDB()/initTheme() 完了まで画面を出さない(getDB()が未初期化で落ちるのを防ぐ)
  const [ready, setReady] = useState(false);
  const { isDark } = useTheme();

  useEffect(() => {
    Promise.all([initDB(), initTheme()]).then(() => setReady(true)).catch(console.error);
  }, []);

  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <StatusBar style={isDark ? 'light' : 'dark'} />
      {ready ? (
        <Stack>
          <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
        </Stack>
      ) : (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
          <ActivityIndicator />
        </View>
      )}
    </GestureHandlerRootView>
  );
}
```

- [ ] **Step 2: 型チェック**

Run: `cd F:\ClaudeWorks\DevelopColorack && npx tsc --noEmit`
Expected: `app/_layout.tsx` に起因するエラーが出ないこと。

- [ ] **Step 3: コミット**

```bash
git add app/_layout.tsx
git commit -m "feat: init theme on startup and sync StatusBar with theme"
```

---

## Task 3: 全消費ファイルを useTheme() 経由に移植する(機械的変換)

**Files:**
- Modify(共通パターン、19ファイル): 下記「対象ファイル一覧」の全ファイル
- Test: なし(型チェックと実機目視確認)

**Interfaces:**
- Consumes: `useTheme(): { colors: typeof lightColors }`(Task 1)

### 変換パターン(全ファイル共通)

各ファイルについて、次の3点を変更する。**スタイルの値自体(色以外のレイアウト定義)は一切変更しない。**

1. `import { colors, radius, spacing, ... } from '../lib/theme';`(または相対パスの深さ違い)から `colors` を削除し、代わりに `import { useTheme, radius, spacing, ... } from '../lib/theme';` とする。
2. コンポーネント関数の先頭(他のフック呼び出しと並べて良い)に `const { colors } = useTheme();` を追加する。
3. モジュール直下にあった `const styles = StyleSheet.create({ ... });` を、コンポーネント関数の内部末尾(return文の直前)に移動し、`useMemo` で包む:
   ```typescript
   const styles = useMemo(() => StyleSheet.create({
     // 既存の中身をそのまま
   }), [colors]);
   ```
   `useMemo` を `react` から追加importする。

### 具体例: components/TypeIcon.tsx(最小ファイルでの完全な変換例)

変換前:
```typescript
// components/TypeIcon.tsx (抜粋)
import { StyleSheet, Text, View } from 'react-native';
import { colors } from '../lib/theme';

export default function TypeIcon({ paintType }: { paintType: string | null | undefined }) {
  return (
    <View style={styles.badge}>
      <Text style={styles.text}>{/* ... */}</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  badge: { /* ... */ backgroundColor: colors.chip },
  text: { /* ... */ color: colors.textMuted },
});
```

変換後:
```typescript
// components/TypeIcon.tsx (抜粋)
import { useMemo } from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { useTheme } from '../lib/theme';

export default function TypeIcon({ paintType }: { paintType: string | null | undefined }) {
  const { colors } = useTheme();
  const styles = useMemo(() => StyleSheet.create({
    badge: { /* ... */ backgroundColor: colors.chip },
    text: { /* ... */ color: colors.textMuted },
  }), [colors]);

  return (
    <View style={styles.badge}>
      <Text style={styles.text}>{/* ... */}</Text>
    </View>
  );
}
```

`StyleSheet.create` の中身(`{ /* ... */ }`部分)は元ファイルの内容をそのまま移すだけで、キーや値は一切変更しない。

### 対象ファイル一覧(TypeIcon以外の18ファイル、同一パターンを適用)

| ファイル | 現在の component開始行 | 現在の StyleSheet.create 行 |
|---|---|---|
| `app/(tabs)/favorites.tsx` | 25 | 77 |
| `app/(tabs)/catalog.tsx` | 24 | 200 |
| `app/(tabs)/wishlist.tsx` | 25 | 77 |
| `app/(tabs)/settings.tsx` | 8 | 65 (Task 4でUI追加も行うためTask 4側で一緒に変換する。ここでは対象外) |
| `app/(tabs)/owned.tsx` | 52 | 329 |
| `components/ColorCameraPicker.tsx` | 19 | 158 |
| `components/FilterModal.tsx` | 32 | 189 |
| `components/ClearableInput.tsx` | 8 | 21 |
| `components/PaintFormModal.tsx` | 32 | 135 |
| `components/AddPaint/BarcodeScanner.tsx` | 21 | 68 |
| `components/PaintRow.tsx` | 28 | 47 |
| `components/AddPaint/ColorMatcher.tsx` | 34 | 106 |
| `components/AddPaint/HierarchyBrowser.tsx` | 34 | 159 |
| `components/AddPaint/index.tsx` | 32 | 105 |
| `components/AddPaint/TextSearch.tsx` | 26 | 70 |

`components/PaintFormFields.tsx` と `components/AddPaint/ManualEntry.tsx` はモジュール共有のヘルパー関数 `optionChip` があるため、この一覧から除外し **Task 4bとは別枠の Task 3b** で個別対応する(下記参照)。

- [ ] **Step 1: 上記14ファイルすべてに変換パターンを適用する**

各ファイルを開き、「変換パターン」の3点を適用する。スタイル定義の中身は変更しない。

- [ ] **Step 2: 型チェック**

Run: `cd F:\ClaudeWorks\DevelopColorack && npx tsc --noEmit`
Expected: 上記14ファイル + Task 1/2で対応済みのファイルにエラーが出ないこと。`components/PaintFormFields.tsx` / `components/AddPaint/ManualEntry.tsx` / `app/(tabs)/settings.tsx` はまだ未対応なのでエラーが残っていて正常。

- [ ] **Step 3: コミット**

```bash
git add app/\(tabs\)/favorites.tsx app/\(tabs\)/catalog.tsx app/\(tabs\)/wishlist.tsx app/\(tabs\)/owned.tsx components/ColorCameraPicker.tsx components/FilterModal.tsx components/ClearableInput.tsx components/PaintFormModal.tsx components/AddPaint/BarcodeScanner.tsx components/PaintRow.tsx components/AddPaint/ColorMatcher.tsx components/AddPaint/HierarchyBrowser.tsx components/AddPaint/index.tsx components/AddPaint/TextSearch.tsx components/TypeIcon.tsx
git commit -m "refactor: migrate screens/components to useTheme() for dark mode"
```

---

## Task 3b: PaintFormFields.tsx と ManualEntry.tsx の個別対応(共有ヘルパー関数の例外処理)

**Files:**
- Modify: `components/PaintFormFields.tsx`
- Modify: `components/AddPaint/ManualEntry.tsx`

**Interfaces:**
- Consumes: `useTheme()`(Task 1)
- Produces: `optionChip(value: string, selected: boolean, label: string, onPress: () => void, chipStyles: { chip: object; chipOn: object; chipText: object; chipTextOn: object }): JSX.Element`(シグネチャ変更、`chipStyles`引数を追加)

`optionChip` はモジュール直下の関数で、これまで `PaintFormFields.tsx` のモジュール直下 `styles`(静的)を暗黙に参照していた。`styles` がコンポーネント内(`useMemo`)に移ると参照できなくなるため、呼び出し側が自分の `styles` を明示的に渡す形に変更する。

- [ ] **Step 1: `components/PaintFormFields.tsx` を書き換える**

`optionChip` のシグネチャに `chipStyles` 引数を追加し、`styles.chip` 等への直接参照をやめて `chipStyles.chip` 等を使う。呼び出し側(`TYPE_OPTIONS.map` / `GLOSS_OPTIONS.map`)は自分の `styles` を渡す。それ以外(`isValidHex`, `TYPE_OPTIONS`, `GLOSS_OPTIONS`)は変更しない。

```typescript
// components/PaintFormFields.tsx
import { Dispatch, SetStateAction, useMemo } from 'react';
import { StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { IconCamera } from '@tabler/icons-react-native';
import { glossLabel } from '../lib/gloss';
import { t } from '../lib/i18n';
import { paintTypeLabel } from '../lib/paintType';
import { useTheme, radius, spacing, touch } from '../lib/theme';
import ClearableInput from './ClearableInput';

export const TYPE_OPTIONS = ['ラッカー塗料', '水性アクリル塗料', 'エナメル塗料', 'エマルジョン塗料'];
export const GLOSS_OPTIONS = ['光沢', '半光沢', 'つや消し', 'メタリック', 'パール'];

export function isValidHex(value: string) {
  return /^#?[0-9a-fA-F]{6}$/.test(value);
}

interface TextField {
  label: string;
  value: string;
  set: Dispatch<SetStateAction<string>>;
}

interface Props {
  fields: TextField[];
  hex: string;
  setHex: Dispatch<SetStateAction<string>>;
  paintType: string | null;
  setPaintType: Dispatch<SetStateAction<string | null>>;
  gloss: string | null;
  setGloss: Dispatch<SetStateAction<string | null>>;
  onOpenCamera: () => void;
  swatch?: boolean;
}

type ChipStyles = { chip: object; chipOn: object; chipText: object; chipTextOn: object };

export function optionChip(value: string, selected: boolean, label: string, onPress: () => void, chipStyles: ChipStyles) {
  return (
    <TouchableOpacity key={value} style={[chipStyles.chip, selected && chipStyles.chipOn]} onPress={onPress}>
      <Text style={[chipStyles.chipText, selected && chipStyles.chipTextOn]}>{label}</Text>
    </TouchableOpacity>
  );
}

export default function PaintFormFields({
  fields, hex, setHex, paintType, setPaintType, gloss, setGloss, onOpenCamera, swatch = false,
}: Props) {
  const { colors } = useTheme();
  const styles = useMemo(() => StyleSheet.create({
    field: { marginBottom: spacing.lg },
    label: { fontSize: 12, color: colors.textMuted, marginBottom: spacing.xs },
    input: { borderWidth: 1, borderColor: colors.border, borderRadius: radius.sm, padding: 10 },
    hexRow: { flexDirection: 'row', alignItems: 'center' },
    hexInput: { flex: 1 },
    previewSwatch: { marginLeft: spacing.md, width: touch.min, height: touch.min, borderRadius: radius.sm, borderWidth: 1, borderColor: colors.border },
    cameraBtn: { marginLeft: spacing.md, width: touch.min, height: touch.min, borderWidth: 1, borderColor: colors.border, borderRadius: radius.sm, alignItems: 'center', justifyContent: 'center' },
    chipRow: { flexDirection: 'row', flexWrap: 'wrap', marginTop: spacing.xs },
    chip: { paddingHorizontal: spacing.lg, paddingVertical: spacing.md, borderRadius: radius.pill, backgroundColor: colors.chip, marginRight: spacing.md, marginBottom: spacing.md },
    chipOn: { backgroundColor: colors.primary },
    chipText: { fontSize: 13, color: colors.textSecondary },
    chipTextOn: { color: colors.onPrimary, fontWeight: 'bold' },
    sectionGap: { marginTop: spacing.lg },
  }), [colors]);

  return (
    <>
      {fields.map(({ label, value, set }) => (
        <View key={label} style={styles.field}>
          <Text style={styles.label}>{label}</Text>
          <ClearableInput style={styles.input} value={value} onChangeText={set} autoCapitalize="none" />
        </View>
      ))}
      <View style={styles.field}>
        <Text style={styles.label}>{t('hex') + ' (#RRGGBB)'}</Text>
        <View style={styles.hexRow}>
          <ClearableInput style={[styles.input, styles.hexInput]} value={hex} onChangeText={setHex} autoCapitalize="none" />
          {swatch && isValidHex(hex) && (
            <View style={[styles.previewSwatch, { backgroundColor: `#${hex.replace('#', '')}` }]} />
          )}
          <TouchableOpacity
            style={styles.cameraBtn}
            onPress={onOpenCamera}
            accessibilityLabel="カメラで色を取得"
          >
            <IconCamera color={colors.primary} size={22} />
          </TouchableOpacity>
        </View>
      </View>

      <Text style={styles.label}>{t('paintType')}</Text>
      <View style={styles.chipRow}>
        {TYPE_OPTIONS.map((v) => optionChip(v, paintType === v, paintTypeLabel(v),
          () => setPaintType(paintType === v ? null : v), styles))}
      </View>

      <Text style={[styles.label, styles.sectionGap]}>{t('gloss')}</Text>
      <View style={styles.chipRow}>
        {GLOSS_OPTIONS.map((v) => optionChip(v, gloss === v, glossLabel(v),
          () => setGloss(gloss === v ? null : v), styles))}
      </View>
    </>
  );
}
```

- [ ] **Step 2: `components/AddPaint/ManualEntry.tsx` を読み、既存の `styles`(21-24行目付近、`StyleSheet.create`)と `chip(...)` 呼び出し(100行目・105行目)を確認する**

Run: `cat -n components/AddPaint/ManualEntry.tsx | sed -n '1,135p'` 相当でファイル全体を確認する。

- [ ] **Step 3: `components/AddPaint/ManualEntry.tsx` を変換パターンに沿って書き換える**

`useTheme()` の取得と `useMemo` 化に加え、既存の `styles`(126行目〜)に `chip` / `chipOn` / `chipText` / `chipTextOn` の4キーを追加する(値は `PaintFormFields.tsx` の対応キーと同一にする)。`chip(...)` の呼び出し2箇所(100行目・105行目)に第5引数として `styles` を追加する:

```typescript
{STATUS_OPTIONS.map((s) => chip(s.key, status === s.key, t(s.label), () => setStatus(s.key), styles))}
...
{boxes.map((b) => chip(String(b.id), boxId === b.id, b.name, () => setBoxId(b.id), styles))}
```

追加する4キーの値:
```typescript
chip: { paddingHorizontal: spacing.lg, paddingVertical: spacing.md, borderRadius: radius.pill, backgroundColor: colors.chip, marginRight: spacing.md, marginBottom: spacing.md },
chipOn: { backgroundColor: colors.primary },
chipText: { fontSize: 13, color: colors.textSecondary },
chipTextOn: { color: colors.onPrimary, fontWeight: 'bold' },
```

- [ ] **Step 4: 型チェック**

Run: `cd F:\ClaudeWorks\DevelopColorack && npx tsc --noEmit`
Expected: `PaintFormFields.tsx` / `ManualEntry.tsx` に起因するエラーが出ないこと。`app/(tabs)/settings.tsx` のみエラーが残っていて正常(Task 4で対応)。

- [ ] **Step 5: コミット**

```bash
git add components/PaintFormFields.tsx components/AddPaint/ManualEntry.tsx
git commit -m "refactor: migrate PaintFormFields/ManualEntry shared chip helper to useTheme()"
```

---

## Task 4: 設定画面にテーマ切替UIを追加する

**Files:**
- Modify: `app/(tabs)/settings.tsx`

**Interfaces:**
- Consumes: `useTheme(): { colors, mode }`, `setThemeMode(mode: ThemeMode): void`, `ThemeMode`(Task 1)

- [ ] **Step 1: `app/(tabs)/settings.tsx` を書き換える**

```typescript
// app/(tabs)/settings.tsx
import { useMemo, useState } from 'react';
import { View, Text, Switch, TouchableOpacity, StyleSheet, Alert } from 'react-native';
import { getDB, setSetting } from '../../lib/db';
import { t, setLocale, getLocale } from '../../lib/i18n';
import { useTheme, setThemeMode, ThemeMode, radius, spacing } from '../../lib/theme';

const THEME_OPTIONS: { value: ThemeMode; labelKey: string }[] = [
  { value: 'light', labelKey: 'themeLight' },
  { value: 'dark', labelKey: 'themeDark' },
  { value: 'system', labelKey: 'themeSystem' },
];

export default function SettingsScreen() {
  const [isJa, setIsJa] = useState(getLocale() === 'ja');
  const { colors, mode } = useTheme();
  const styles = useMemo(() => StyleSheet.create({
    container: { flex: 1, padding: spacing.xl, backgroundColor: colors.surface },
    section: { marginBottom: spacing.xxl },
    sectionTitle: { fontSize: 18, fontWeight: 'bold', marginBottom: spacing.md, color: colors.text },
    langRow: { flexDirection: 'row', alignItems: 'center' },
    themeRow: { flexDirection: 'row' },
    themeBtn: { flex: 1, padding: spacing.lg, borderRadius: radius.sm, backgroundColor: colors.chip, marginRight: spacing.md, alignItems: 'center' },
    themeBtnOn: { backgroundColor: colors.primary },
    themeBtnText: { color: colors.textSecondary },
    themeBtnTextOn: { color: colors.onPrimary, fontWeight: 'bold' },
    resetBtn: { backgroundColor: colors.dangerSoft, borderRadius: radius.sm, padding: spacing.lg, marginBottom: spacing.md },
    resetBtnText: { color: colors.danger, fontWeight: 'bold', textAlign: 'center' },
  }), [colors]);

  const toggleLang = (val: boolean) => {
    setIsJa(val);
    setLocale(val ? 'ja' : 'en');
  };

  const confirmReset = (title: string, onConfirm: () => Promise<void>) => {
    Alert.alert(title, t('resetConfirmMessage'), [
      { text: t('cancel'), style: 'cancel' },
      { text: t('reset'), style: 'destructive', onPress: onConfirm },
    ]);
  };

  const resetOwned = () => confirmReset(t('resetOwned'), async () => {
    const db = getDB();
    await db.runAsync('DELETE FROM inventory');
    await db.runAsync('DELETE FROM boxes');
    const res = await db.runAsync('INSERT INTO boxes (name) VALUES (?)', ['Box']);
    await setSetting('default_box_id', String(res.lastInsertRowId));
  });

  const resetFavorites = () => confirmReset(t('resetFavorites'), async () => {
    await getDB().runAsync("DELETE FROM lists WHERE type = 'favorites'");
  });

  const resetWishlist = () => confirmReset(t('resetWishlist'), async () => {
    await getDB().runAsync("DELETE FROM lists WHERE type = 'wishlist'");
  });

  return (
    <View style={styles.container}>
      <View style={styles.section}>
        <Text style={styles.sectionTitle}>{t('language')}</Text>
        <View style={styles.langRow}>
          <Text style={{ color: colors.text }}>EN</Text>
          <Switch value={isJa} onValueChange={toggleLang} style={{ marginHorizontal: 8 }} />
          <Text style={{ color: colors.text }}>JA</Text>
        </View>
      </View>
      <View style={styles.section}>
        <Text style={styles.sectionTitle}>{t('theme')}</Text>
        <View style={styles.themeRow}>
          {THEME_OPTIONS.map((opt) => (
            <TouchableOpacity
              key={opt.value}
              style={[styles.themeBtn, mode === opt.value && styles.themeBtnOn]}
              onPress={() => setThemeMode(opt.value)}
            >
              <Text style={[styles.themeBtnText, mode === opt.value && styles.themeBtnTextOn]}>{t(opt.labelKey)}</Text>
            </TouchableOpacity>
          ))}
        </View>
      </View>
      <View style={styles.section}>
        <Text style={styles.sectionTitle}>{t('reset')}</Text>
        <TouchableOpacity style={styles.resetBtn} onPress={resetOwned}>
          <Text style={styles.resetBtnText}>{t('resetOwned')}</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.resetBtn} onPress={resetFavorites}>
          <Text style={styles.resetBtnText}>{t('resetFavorites')}</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.resetBtn} onPress={resetWishlist}>
          <Text style={styles.resetBtnText}>{t('resetWishlist')}</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}
```

- [ ] **Step 2: `translations/ja.json` と `translations/en.json` に4キーを追加する**

`translations/ja.json` に追加:
```json
"theme": "テーマ",
"themeLight": "ライト",
"themeDark": "ダーク",
"themeSystem": "システムに従う"
```

`translations/en.json` に追加:
```json
"theme": "Theme",
"themeLight": "Light",
"themeDark": "Dark",
"themeSystem": "System"
```

(既存のJSONオブジェクトの末尾に追加する。カンマの付け忘れに注意。)

- [ ] **Step 3: 型チェック**

Run: `cd F:\ClaudeWorks\DevelopColorack && npx tsc --noEmit`
Expected: エラー0件(全ファイルの移植が完了している状態)。

- [ ] **Step 4: コミット**

```bash
git add "app/(tabs)/settings.tsx" translations/ja.json translations/en.json
git commit -m "feat: add theme toggle UI to settings screen"
```

---

## Task 5: 実機確認(Expo Go)

**Files:** なし(手動確認のみ)

- [ ] **Step 1: Expo Goサーバーを起動する**

Run: `cd F:\ClaudeWorks\DevelopColorack && npm run start:go`

- [ ] **Step 2: iPhone実機のExpo Goで以下を確認する**

1. 設定画面で「ライト」「ダーク」「システムに従う」を切り替え、owned/catalog/favorites/wishlist全画面の配色が即座に切り替わることを確認する。
2. 「システムに従う」を選択した状態で、iPhone本体の「設定 > 画面表示と明るさ」でライト/ダークを切り替え、アプリが追従することを確認する。
3. 「ダーク」を選択した状態でExpo Goアプリを完全終了し、再度開いて選択が保持されていることを確認する。
4. 塗料追加モーダル(手動入力/階層ブラウザ/テキスト検索/バーコード/色マッチング)の各タブをダークモードで開き、テキストが読めること(白背景に白文字のような問題がないこと)を確認する。

- [ ] **Step 3: 問題があれば該当ファイルの色定義(`lightColors`/`darkColors`)を調整し、再度Step 2を確認する**

- [ ] **Step 4: mainブランチへのPRを作成する準備をする(プロジェクトルール: PR必須)**

```bash
git push -u origin feature/dark-mode
gh pr create --title "ダークモード対応" --body "$(cat <<'EOF'
## Summary
- lib/theme.ts にライト/ダークの2パレットとuseTheme()フックを追加
- app_settings にtheme_modeを永続化し、設定画面でライト/ダーク/システム追従を切替可能に
- 全画面のStyleSheetをコンポーネント内useMemo化してテーマ切替に追従

## Test plan
- [ ] iPhone実機のExpo Goで設定画面からのモード切替を確認
- [ ] システム追従モードで端末側ダークモード切替に追従することを確認
- [ ] アプリ再起動後も選択したモードが保持されることを確認
EOF
)"
```

---

## Self-Review Notes

- **Spec coverage:** 切替方式(手動+システム追従)→Task1/4、永続化→Task1/2、パレット分割→Task1、画面移植→Task3/3b、設定UI→Task4、StatusBar→Task2、テスト(assert自己チェック)→Task1、実機確認→Task5。全項目に対応するタスクあり。
- **Type consistency:** `useTheme()` の戻り値型 `{ colors, mode, isDark }` は Task1で定義し、Task2(`isDark`)・Task3/3b(`colors`)・Task4(`colors`, `mode`)で一貫して使用している。`ThemeMode` 型・`setThemeMode` のシグネチャもTask1定義のままTask4で使用。
- **除外事項:** `App.tsx`(expo-router未使用のボイラープレート)、`components/AdBanner.tsx`・`components/SwipeBack.tsx`(colors不使用)は対象外。
